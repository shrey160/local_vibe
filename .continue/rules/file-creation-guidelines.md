---
globs: '["**/*.html", "**/*.css", "**/*.js", "**/*.json", "**/.*"]'
description: This rule applies whenever there is a request for folder creation,
  file creation, or any implementation that requires generating code files and
  folders.
alwaysApply: false
---

When a user requests folder or file creation, ALWAYS provide clear step-by-step instructions on how to create them. Always generate the complete content for all required files rather than just providing partial code or templates. Ensure every file needed for the requested feature is fully implemented with ready-to-use code.